name = "my-birthday-funding"
pages_build_output_dir = "dist"
compatibility_date = "2024-12-01"

# 로컬 개발 시에는 .dev.vars 파일에 DATABASE_URL 을 넣으세요.
# 배포 환경의 DATABASE_URL 은 Cloudflare 대시보드 또는 아래 명령으로 설정합니다.
#   npx wrangler pages secret put DATABASE_URL
